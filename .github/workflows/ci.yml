name: CI

# Trigger configuration
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.11"]

    steps:
    - name: Check out repository
      uses: actions/checkout@v5

    - name: Set up Python ${{ matrix.python-version }}
      id: setup-python # This ID is required to reference the step's outputs later
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        virtualenvs-create: true
        virtualenvs-in-project: true

    - name: Load cached venv
      id: cached-poetry-dependencies
      uses: actions/cache@v5
      with:
        path: .venv
        key: venv-${{ runner.os }}-${{ steps.setup-python.outputs.python-version }}-${{ hashFiles('**/poetry.lock') }}

    - name: Install clipboard backend (Linux)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y xclip

    - name: Install dependencies
      run: poetry install --no-interaction

    - name: Run all pre-commit checks (Lint, Format, Types, SLOC, Pyramid, Tests)
      run: poetry run pre-commit run --all-files

    - name: Run Pip-Audit (Dependency Vulnerability Scan)
      run: poetry run pip-audit

    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '24'

    - name: Install jscpd
      run: npm install -g jscpd

    - name: Run jscpd (Code Duplication Detection)
      run: jscpd --min-tokens=50 --reporters=console --threshold 0 src
