# spikes/functional/03-research-action-contract/proposal.yaml
# ---
# Final, approved contract for the `research` action, matching the user's specification.

- action: research
  # The 'description' is optional metadata for the human operator.
  description: "Discover official documentation for Python's logging library."
  # The 'queries' parameter is a multi-line string. Each line represents a
  # separate search query to be executed.
  queries: |
    python logging file rotation example site:docs.python.org
    python logging cookbook
