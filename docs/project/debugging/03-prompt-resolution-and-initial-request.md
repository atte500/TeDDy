# Bug: PromptManager Resolution Failure & Duplicate "Initial Request"
- **Status:** Resolved
- **Milestone:** [N/A](/docs/project/PROJECT.md)
- **Vertical Slice:** N/A
- **Specs:** N/A

## Symptoms

### Symptom 1: "searched None and None" warning
- **Expected:** When starting a session, the system prompt for the agent should be resolved correctly (e.g., `pathfinder.xml`).
- **Actual:** A warning is logged: `PromptManager: Failed to resolve system prompt for agent 'Pathfinder' (searched None and None)`. The agent name uses capital P (`Pathfinder`) while the prompt file is `pathfinder.xml` (lowercase p). This causes `_find_prompt_file` to fail to match the stem.
- **Reproduction:** Run `teddy start` and observe the warning in the console.

### Symptom 2: "Initial Request:" printed twice
- **Expected:** The "Initial Request:" header and its content should appear exactly once during session start.
- **Actual:** The output shows "Initial Request:" twice (first during session creation output, then again after the turn header). The user message content also appears duplicated.
- **Reproduction:** Run `teddy start` with a message and observe the console output.

## Context & Scope

### Regressing Delta
Both symptoms are likely present from the introduction of the session workflow and `_print_initial_request` function. No specific recent regression identified yet.

### Environmental Triggers
- Occurs in all environments where a session is started interactively.
- The prompt resolution failure depends on the agent name being capitalized (likely from `plan.metadata` derived from a user-provided or default agent name).

### Ruled Out
- N/A – still gathering data.

## Diagnostic Analysis

### Causal Model

#### Symptom 1: "searched None and None" warning
- `PromptManager.fetch_system_prompt()` receives `agent_name = 'Pathfinder'` (capital P).
- `_find_prompt_file()` searches `.teddy/prompts/` for a file whose `Path(f).stem == agent_name`.
- The prompt file is `pathfinder.xml` (stem `pathfinder`, lowercase p).
- The equality check is case-sensitive → fails, returns `None`.
- Both `session_root_prompt` and `teddy_prompt_path` are `None` → warning logs "searched None and None".
- The agent name capitalization likely comes from user's `.teddy/config.yaml` or CLI `-a` flag (not from code transformation).

#### Symptom 2: Duplicate "Initial Request:" output
- `_print_initial_request` is called from two distinct code paths for the first turn:
  1. `SessionLifecycleManager._handle_planning_and_execution()` (line where `turn_number == "01"`) — called BEFORE planning.
  2. `SessionOrchestrator.execute()` (lines 184/187) — called AFTER planning, during execution.
- Both calls succeed and print the same content, resulting in duplicate output.
- Subsequent turns (02+) only get the call from the orchestrator, so the duplication is limited to the first turn.

### Discrepancies
- The `_print_initial_request` function is called only once in `session_orchestrator.execute()`, but the user sees two prints. This suggests a second call from outside `execute()` (possibly in the `start` command handler or `planning_service`). (Resolved: Found second call site in `SessionLifecycleManager._handle_planning_and_execution()` for turn "01". MRE confirmed 2 calls.)
- The prompt warning is generated by `PromptManager.fetch_system_prompt()` with `agent_name = 'Pathfinder'` (capital P), while the canonical prompt file is `pathfinder.xml` (lowercase p). The string equality check `Path(f).stem == agent_name` fails. (Resolved: MRE confirmed the warning output with reason.)

### Investigation History
1. grep for "Failed to resolve system prompt" found at `prompt_manager.py:99`. grep for "Initial Request:" found at `session_orchestrator.py:39:59`. Grep for "searched None" returned no results (the literal "None" is passed as a variable, not a string literal).
2. Read `session_lifecycle_manager.py` — found second call to `_print_initial_request` in `_handle_planning_and_execution` for turn "01". This is the duplicate call site.
3. Search for `.title()` on agent names — found only in jinja template (display) and `provider.capitalize()` (unrelated). No code transformation on agent name in core services.
4. MRE for prompt case mismatch confirmed warning output with `agent_name='Pathfinder'` against lowercase prompt file.
5. MRE for initial request confirmed 2 secho calls when simulating lifecycle + orchestrator flow.
6. **Shadow verification (prompt case):** Created `shadow_prompt_manager.py` with `_find_prompt_file` using `.casefold()` instead of `==`. After fixing the mock's directory detection, the MRE confirmed that both "Pathfinder" and "pathfinder" return `"You are a pathfinder agent."` with no warning. This empirically proves the `.casefold()` fix resolves the case sensitivity bug.
7. **Shadow verification (initial request):** Confirmed that removing the `_print_initial_request` call from `SessionLifecycleManager._handle_planning_and_execution` reduces output to a single "Initial Request:" line.

## Solution

### Root Cause 1: Case-Sensitive Prompt File Matching
`PromptManager._find_prompt_file` compares `Path(f).stem == agent_name` (case-sensitive). When the agent name is provided with a capital letter (e.g., "Pathfinder" from user config or CLI) but the prompt file is lowercase ("pathfinder.xml"), the comparison fails, returning `None`.

**Fix:** Change `==` to `.casefold()` comparison in `_find_prompt_file`:
```python
if Path(f).stem.casefold() == agent_name.casefold():
```

This aligns with `get_available_agents` which already uses `.casefold()` for sorting.

### Root Cause 2: Duplicate "Initial Request:" Output
`_print_initial_request` is called from two locations for the first turn:
1. `SessionLifecycleManager._handle_planning_and_execution()` before planning.
2. `SessionOrchestrator.execute()` during execution.

**Fix:** Remove the call from `SessionLifecycleManager._handle_planning_and_execution()`. The orchestrator already handles printing the initial request for all turns, including the first. The lifecycle manager's call was redundant.

### Preventative Measures
- **Case normalization policy:** Establish a convention that agent names should be treated case-insensitively across the codebase. `_find_prompt_file` was the only location with a strict equality check; all other comparisons (sorting, display) already used casefold or case-insensitive methods.
- **Code review guard:** Any new comparison between user-provided agent names and filesystem stems must use casefold or lowercasing.
- **Lifecycle duplication:** When adding new output in session lifecycle, audit whether the same output is already produced by the orchestrator or planner to avoid duplication.
