# TeDDy v0.1.6

Features:
- Rename prototyper agent to Behavioral Prototype & Spec Validator
- Integrate first-principles thinking into Pathfinder mission analysis
- Show full git status in input.md while preserving short format for per-file metadata
- Auto-install pre-commit hooks and git init with green notification banners
- Start background version check thread at session begin
- Improve update messaging for experimental version users

Fixes:
- Harvest action content from pending temp file before TUI execution
- Strip non-alphabetic characters from action type names
- Fix cross-platform absolute path detection in regression tests
- Check CWD only for git detection instead of walking parent directories
- Skip callback summary when init subcommand is invoked
- Uninstall before install for stable release switch
- Always show stable version update for prerelease users
- Add explicit tld>=0.10 dependency to prevent import shadowing conflict
- Tighten tld constraint to <1.0 to prevent rogue package shadowing
- Correct direct-execution vs handoff-to-assistant logic in Pathfinder
- Pass cache_dir to get_context to enable web caching during planning
