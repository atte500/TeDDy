# TeDDy v0.1.11

Fixes:
- Decode percent-encoded sequences in `normalize_path` for proper file path handling
- Safely coerce action parameters with best-effort number extraction for parser robustness
- Wrap `get_content` in action factory with `try/except` to prevent cascading error propagation
- Normalize agent name comparisons (case-insensitive matching) and context paths from the `-c` flag for consistent behavior
- Emit yellow warning on health check subprocess failure and guard `shutil.which` calls
- Update upgrade instructions from `pipx` to `uv`
