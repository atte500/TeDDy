# TeDDy v0.1.9

Features:
- Port bash pre-commit hooks to Python for Windows compatibility
- Add `--force` flag to experimental upgrade command

Fixes:
- Prevent duplicate MESSAGE reply in execution orchestrator
- Fix Python subprocess initialization in shell adapter
- Update acceptance test expectations for `--pre` flag in experimental upgrade message
