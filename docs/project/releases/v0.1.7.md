# TeDDy v0.1.7

Features:
- Migrate installation and upgrade instructions from pip to pipx
- Add "Install pipx" prerequisite step to README
- Update internal `teddy update` command to display `pipx upgrade` instructions
- Add cross-platform post-commit hook (Python-based, compatible with Windows and UNIX)
- Add pipx-powered experimental install instructions
- Install dev dependencies to enable post-commit test gate

Fixes:
- Fix post-commit hook execution on Windows (replaced bash script with Python script)
- Suppress detect-secrets false positive for API key placeholder in README
