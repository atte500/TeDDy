# TeDDy v0.1.8

Features:
- Migrate build system, CI/CD, developer workflow, and user docs from poetry/pipx to uv
- Update `teddy update` command to display `uv tool upgrade teddy-cli` (stable) and `uv tool install` with TestPyPI flags (experimental)
- Update README install instructions to use `uv tool install teddy-cli`

Fixes:
- Fix test harness to work with typer CliRunner API change (removed `mix_stderr` parameter)
- Suppress detect-secrets false positive for API key placeholder in README
- Skip DI boundary enforcement tests gracefully on Windows (grep unavailable)
- Remove dead config key `auto_update` from config.yaml
- Remove dead code (`perform_upgrade`, `should_update` in update_checker)