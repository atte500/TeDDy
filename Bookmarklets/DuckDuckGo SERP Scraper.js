javascript: (function () { var markdown = "";  /* Target the main result articles */  var results = document.querySelectorAll("article[data-testid='result']"); results.forEach(function (result) {    /* Find the anchor tag inside the h2 (the main title link) */    var linkEl = result.querySelector("h2 a"); if (linkEl) { var title = linkEl.innerText.trim(); var href = linkEl.href;            /* Skip empty results */      if (title && href) { markdown += "- [" + title + "](" + href + ")\n"; } } }); if (markdown) { navigator.clipboard.writeText(markdown).then(function () { alert('DuckDuckGo results copied to clipboard!'); }, function (err) { console.error('Could not copy: ', err); prompt("Copy manually:", markdown); }); } else { alert("No results found. The layout might have changed."); } })();